{
  "name": "frameon-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently \"npm run dev -w @frameon/api\" \"npm run dev -w @frameon/bot\" \"npm run dev -w @frameon/scheduler\" \"npm run dev -w @frameon/worker\" \"npm run dev -w @frameon/miniapp\"",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "concurrently": "^10.0.3",
    "dotenv": "^17.4.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^6.0.3"
  }
}
